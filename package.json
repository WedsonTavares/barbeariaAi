{
  "name": "barbearia-ai",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "engines": { "node": ">=24" },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "db:generate": "pnpm --filter @barbearia-ai/core db:generate",
    "db:migrate": "pnpm --filter @barbearia-ai/core db:migrate",
    "db:deploy": "pnpm --filter @barbearia-ai/core db:deploy",
    "db:rls": "pnpm --filter @barbearia-ai/core db:rls",
    "db:seed": "pnpm --filter @barbearia-ai/core db:seed",
    "worker": "pnpm --filter @barbearia-ai/worker start"
  },
  "devDependencies": {
    "turbo": "^2.1.0",
    "typescript": "^5.9.0",
    "@types/node": "^24.0.0"
  },
  "pnpm": {
    "overrides": {
      "ioredis": "5.11.1"
    }
  }
}
