# Banco de TESTE — NUNCA aponte isto para o projeto de produção.
# Copie para packages/core/.env.test e preencha com um projeto Supabase separado
# (ou Postgres local). `vitest.config.ts` recusa rodar sem este arquivo.
DATABASE_URL="postgresql://app_runtime:SENHA@HOST:6543/postgres?pgbouncer=true&sslmode=require"
DIRECT_URL="postgresql://postgres:SENHA@HOST:5432/postgres?sslmode=require"
